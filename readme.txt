Имя бота: Ffirst

В целом было интересно и полезно. 
Однако, требовать называть переменные как указано в тз это перебор(имхо)
Я имею ввиду вот этот блок: 
"
- имя валюты, цену на которую надо узнать, — base;
- имя валюты, цену в которой надо узнать, — quote; 
- количество переводимой валюты — amount.
"
Ps: метод split() по умолчанию подразумевает пробел.